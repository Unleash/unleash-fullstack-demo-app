# Logs.
logs
*.log*

# Dependencies and Build Assets.
node_modules
dist
dist-ssr

# Settings and environment variables.
*.local
.env

# `yarn` is retired; `pnpm-lock.yaml` and `package-lock.json` are now canonical.
yarn.lock

# IDEs/editors directories and files.
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS-level stuff.
.DS_Store

# Tests Assets (Playwright).
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
